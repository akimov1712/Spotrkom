<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Spotrkom"
        tools:targetApi="31">
        <activity
            android:name=".screens.activities.autorization.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".screens.activities.autorization.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".screens.activities.autorization.AutorizationActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".screens.activities.main.TableUserActivity"
            android:exported="false" />
        <activity
            android:name=".screens.activities.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>